# 浅拷贝

## 概念
    1、浅拷贝是创建一个对象，该对象有着原始对象属性值的一份精确拷贝。
    2、如果属性是基本类型，拷贝的就是基本类型的值。
    3、如果属性是引用类型，拷贝的就是引用类型的内存地址。
    4、因此，原始对象中引用类型属性值改变，新对象中引用类型的属性值也会发生改变。

## 浅拷贝有哪些方法？

### 对象浅拷贝

#### 1. Object.assign()
`Object.assign()` 是 ES6 提供的方法，用于将所有可枚举属性的值从一个或多个源对象复制到目标对象。

```javascript
const original = {
    name: '张三',
    age: 25,
    hobbies: ['读书', '游泳'],
    address: {
        city: '北京',
        district: '朝阳区'
    }
};

// 使用 Object.assign() 进行浅拷贝
const copied = Object.assign({}, original);

console.log(copied); // { name: '张三', age: 25, hobbies: ['读书', '游泳'], address: {...} }

// 修改引用类型属性
original.hobbies.push('跑步');
original.address.city = '上海';

console.log(copied.hobbies); // ['读书', '游泳', '跑步'] - 受影响
console.log(copied.address.city); // '上海' - 受影响
```

#### 2. 展开运算符 (Spread Operator)
ES6 的展开运算符 `...` 可以用于对象的浅拷贝。

```javascript
const original = {
    name: '李四',
    age: 30,
    skills: ['JavaScript', 'React'],
    profile: {
        level: 'Senior',
        experience: '5年'
    }
};

// 使用展开运算符进行浅拷贝
const copied = { ...original };

console.log(copied); // 完整的对象副本

// 测试浅拷贝特性
original.skills.push('Vue');
original.profile.level = 'Expert';

console.log(copied.skills); // ['JavaScript', 'React', 'Vue'] - 受影响
console.log(copied.profile.level); // 'Expert' - 受影响
```

### 数组浅拷贝

#### 1. Array.prototype.slice()
对于数组，可以使用 `slice()` 方法进行浅拷贝。

```javascript
const originalArray = [1, 2, 3, { name: '测试' }, [4, 5]];

// 使用 slice() 进行浅拷贝
const copiedArray = originalArray.slice();

console.log(copiedArray); // [1, 2, 3, { name: '测试' }, [4, 5]]

// 修改原始数组中的引用类型
originalArray[3].name = '修改后的测试';
originalArray[4].push(6);

console.log(copiedArray[3].name); // '修改后的测试' - 受影响
console.log(copiedArray[4]); // [4, 5, 6] - 受影响
```

#### 2. Array.prototype.concat()
使用 `concat()` 方法也可以实现数组的浅拷贝。

```javascript
const originalArray = [1, 2, { name: '对象' }, [3, 4]];

// 使用 concat() 进行浅拷贝
const copiedArray = originalArray.concat();

console.log(copiedArray); // [1, 2, { name: '对象' }, [3, 4]]

// 测试浅拷贝特性
originalArray[2].name = '修改的对象';
originalArray[3].push(5);

console.log(copiedArray[2].name); // '修改的对象' - 受影响
console.log(copiedArray[3]); // [3, 4, 5] - 受影响
```

### 手动实现-浅拷贝 !

```javascript

// 手动实现浅拷贝步骤：
// 1. 判断参数是否为基本类型（如：string、number、boolean、null、undefined），如果是则直接返回该值
// 2. 如果是引用类型（对象或数组），则创建一个新的对象或数组
// 3. 遍历原对象的自身属性（不包括原型链上的属性）
// 4. 将每个属性的值直接赋值给新对象（如果属性值是引用类型，复制的是引用地址）
// 5. 返回新对象

function shallowCopy(obj) {

    // 1、用typeof判断参数类型
    // 这里把null单独拿出来是因为 typeof null 会返回 object，所以要先判断null。
    if (obj === null || typeof obj !== "object") {
        return obj;
    }

    // 2、用isArray判断是数组还是对象
    const newObj = Array.isArray(obj) ? [] : {};

    // 3、for in 开始遍历对象的所有属性
    for (let key in obj) {

        // 只复制对象自身属性。
        // 为啥只复制自身属性？因为如果不这样做，原型链上的属性也会被拷贝到新对象里，这样新对象就不再是原对象“自身”属性的简单副本了，可能会引入意料之外的属性，导致数据混乱。
        if (obj.hasOwnProperty(key)) {
            // 将对象的属性直接赋值，引用类型赋值的是引用地址
            newObj[key] = obj[key];
        }
    }

    // 4、返回新对象
    return newObj;

}


// 测试用例
const obj = {
    name: '张三',
    age: '28',
    hobby: ['篮球','足球','羽毛球'],
    info: {
        city:'济南'
    },
}

// 调用函数
const newObj = shallowCopy(obj);

// 补充测试1：修改新对象的基本类型属性，不影响原对象
newObj.name = '李四';
console.log('修改newObj.name后：');
console.log('原对象name:', obj.name); // 期望输出：张三
console.log('新对象name:', newObj.name); // 期望输出：李四

// 补充测试2：修改新对象的引用类型属性，会影响原对象
newObj.hobby.push('乒乓球');
console.log('修改newObj.hobby后：');
console.log('原对象hobby:', obj.hobby); // 期望输出：['篮球','足球','羽毛球','乒乓球']
console.log('新对象hobby:', newObj.hobby); // 期望输出：['篮球','足球','羽毛球','乒乓球']

```

## 浅拷贝的特点总结

1. **基本类型**：完全独立，修改不会相互影响
2. **引用类型**：共享内存地址，修改会相互影响
3. **性能**：比深拷贝性能更好，内存占用更少
4. **适用场景**：当只需要复制对象的第一层属性时使用

## 使用建议

- **对象浅拷贝**：推荐使用 `Object.assign()` 或展开运算符 `...`
- **数组浅拷贝**：推荐使用 `slice()` 或 `concat()` 方法
