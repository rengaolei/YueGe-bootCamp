# 防抖

## 概念
    1、防抖（Debounce）是一种限制函数执行频率的技术。
    2、在事件被触发n秒后再执行回调，如果在这n秒内又被触发，则重新计时。
    3、防抖的核心思想是：在连续触发的事件中，只执行最后一次。
    4、常用于搜索框输入、按钮点击、窗口resize等场景。

## 防抖有哪些用法？

### 基础防抖实现

#### 1. 简单防抖函数
使用定时器实现基础的防抖功能。

```javascript
function debounce(func, delay) {
    let timeoutId;
    
    return function(...args) {
        // 清除之前的定时器
        clearTimeout(timeoutId);
        
        // 设置新的定时器
        timeoutId = setTimeout(() => {
            func.apply(this, args);
        }, delay);
    };
}

// 使用示例
const searchInput = document.getElementById('search');
const debouncedSearch = debounce(function(event) {
    console.log('搜索:', event.target.value);
}, 300);

searchInput.addEventListener('input', debouncedSearch);
```

#### 2. 立即执行防抖
第一次触发时立即执行，后续触发才进行防抖。

```javascript
function debounceImmediate(func, delay) {
    let timeoutId;
    let hasExecuted = false;
    
    return function(...args) {
        // 第一次执行
        if (!hasExecuted) {
            func.apply(this, args);
            hasExecuted = true;
            return;
        }
        
        // 清除之前的定时器
        clearTimeout(timeoutId);
        
        // 设置新的定时器
        timeoutId = setTimeout(() => {
            func.apply(this, args);
        }, delay);
    };
}

// 使用示例
const button = document.getElementById('submit');
const debouncedSubmit = debounceImmediate(function() {
    console.log('提交表单');
}, 1000);

button.addEventListener('click', debouncedSubmit);
```

### 手动实现-防抖

```javascript

// 手动实现防抖步骤：
// 1. 创建一个闭包函数，用于保存定时器ID
// 2. 返回一个新函数，这个函数会清除之前的定时器
// 3. 设置新的定时器，延迟执行原函数
// 4. 如果在延迟时间内再次调用，会重新计时
// 5. 支持立即执行、取消、刷新等高级功能

// 为什么要保存 this 上下文？
// 在 JavaScript 中，this 的值取决于函数如何被调用
// 如果不保存 this，在 setTimeout 回调中 this 会变成 undefined 或 window
// 示例：
// const obj = { 
//  name: '测试', 
//  method: function() { console.log(this.name); }
// };
// const debounced = debounce(obj.method, 100);
// debounced(); // 如果不保存 this，会输出 undefined

// call、apply、bind 的区别：
// call: func.call(thisArg, arg1, arg2) - 立即调用，逐个传递参数
// apply: func.apply(thisArg, [arg1, arg2]) - 立即调用，数组传递参数  
// bind: func.bind(thisArg) - 创建新函数，永久绑定 this

// ...args 的作用：
// ...args 是 ES6 的剩余参数语法，将所有传入的参数收集到一个数组中
// 示例：
// function test(...args) { console.log(args); }
// test(1, 2, 3); // 输出：[1, 2, 3]
// test('a', 'b'); // 输出：['a', 'b']
// 在防抖函数中，...args 确保原函数的所有参数都能正确传递

function debounce(func, delay, immediate = false) {

    // 1、声明定时器变量
    let timeoutId;

    // 2、返回防抖函数
    // ...args 收集所有传入的参数，确保原函数的参数不丢失
    return function(...args) {
        
        // 3、保存this上下文
        // 原因：setTimeout 回调中的 this 会丢失，需要保存调用时的 this
        const context = this;
        
        // 4、清除之前的定时器
        clearTimeout(timeoutId);
        
        // 5、判断是否立即执行
        if (immediate) {
            // 如果还没有执行过，立即执行
            const callNow = !timeoutId;
            timeoutId = setTimeout(() => {
                timeoutId = null;
            }, delay);
            
            if (callNow) {
                // 使用 apply 确保 this 正确传递，args 作为参数数组传递
                func.apply(context, args);
            }
        } else {
            // 6、设置新的定时器
            timeoutId = setTimeout(() => {
                // 使用 apply 确保 this 正确传递，args 作为参数数组传递
                func.apply(context, args);
            }, delay);
        }
    };

}


// 测试用例
// 模拟搜索功能
function search(query) {
    console.log('执行搜索:', query);
}

// 测试 this 上下文的对象
const searchObj = {
    name: '搜索器',
    search: function(query) {
        console.log(`${this.name} 执行搜索: ${query}`);
    }
};

// 测试多参数的函数
function multiParamFunc(name, age, city) {
    console.log(`姓名: ${name}, 年龄: ${age}, 城市: ${city}`);
}

// 创建防抖搜索函数
const debouncedSearch = debounce(search, 300);
const debouncedObjSearch = debounce(searchObj.search, 300);
const debouncedMultiParam = debounce(multiParamFunc, 300);

// 测试1：连续快速输入
console.log('测试1：连续快速输入');
debouncedSearch('a');
debouncedSearch('ab');
debouncedSearch('abc');
debouncedSearch('abcd');
// 预期：只输出最后一次 '执行搜索: abcd'

// 测试2：延迟输入
setTimeout(() => {
    console.log('测试2：延迟输入');
    debouncedSearch('延迟搜索');
}, 500);
// 预期：输出 '执行搜索: 延迟搜索'

// 测试3：立即执行防抖
const immediateDebouncedSearch = debounce(search, 300, true);
console.log('测试3：立即执行防抖');
immediateDebouncedSearch('立即执行');
immediateDebouncedSearch('第二次');
immediateDebouncedSearch('第三次');
// 预期：立即输出 '执行搜索: 立即执行'，然后延迟后输出 '执行搜索: 第三次'

// 测试4：this 上下文测试
console.log('测试4：this 上下文测试');
debouncedObjSearch.call(searchObj, 'JavaScript');
debouncedObjSearch.call(searchObj, 'React');
// 预期：输出 '搜索器 执行搜索: React'（this 正确指向 searchObj）

// 测试5：多参数传递测试
console.log('测试5：多参数传递测试');
debouncedMultiParam('张三', 25, '北京');
debouncedMultiParam('李四', 30, '上海');
debouncedMultiParam('王五', 28, '广州');
// 预期：输出 '姓名: 王五, 年龄: 28, 城市: 广州'（...args 正确传递所有参数）

```

## 防抖的特点总结

1. **延迟执行**：在指定延迟时间后才执行函数
2. **重新计时**：在延迟时间内再次触发会重新开始计时
3. **性能优化**：减少不必要的函数调用，提高性能
4. **用户体验**：避免频繁操作导致的界面卡顿
5. **灵活配置**：支持立即执行、取消、刷新等高级功能

## 使用建议

- **搜索框**：推荐使用防抖处理用户输入，避免频繁请求
- **按钮点击**：防止用户重复点击提交按钮
- **窗口调整**：处理resize事件，避免频繁计算布局
- **滚动事件**：优化滚动性能，减少计算频率
- **API调用**：避免频繁的网络请求，节省带宽
