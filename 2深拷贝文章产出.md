# 深拷贝

## 概念
    1、深拷贝是创建一个对象，该对象有着原始对象属性值的一份完全独立的拷贝。
    2、如果属性是基本类型，拷贝的就是基本类型的值。
    3、如果属性是引用类型，会递归地拷贝引用类型的所有层级，创建全新的对象。
    4、因此，原始对象中任何属性值改变，都不会影响新对象中的属性值。

## 深拷贝有哪些方法？

### 对象深拷贝

#### 1. JSON.parse(JSON.stringify())
使用 JSON 方法进行深拷贝，这是最简单的方法，但有一些限制。

```javascript
const original = {
    name: '张三',
    age: 25,
    hobbies: ['读书', '游泳'],
    address: {
        city: '北京',
        district: '朝阳区'
    }
};

// 使用 JSON 方法进行深拷贝
const copied = JSON.parse(JSON.stringify(original));

console.log(copied); // { name: '张三', age: 25, hobbies: ['读书', '游泳'], address: {...} }

// 修改引用类型属性
original.hobbies.push('跑步');
original.address.city = '上海';

console.log(copied.hobbies); // ['读书', '游泳'] - 不受影响
console.log(copied.address.city); // '北京' - 不受影响
```

**JSON 方法的限制：**
- 不能处理函数、undefined、Symbol
- 不能处理 Date 对象（会转为字符串）
- 不能处理 RegExp、Error 对象
- 不能处理循环引用

#### 2. 使用第三方库 Lodash
Lodash 提供了完善的深拷贝方法 `cloneDeep`。

```javascript
// 需要先安装 lodash: npm install lodash
const _ = require('lodash');

const original = {
    name: '李四',
    age: 30,
    skills: ['JavaScript', 'React'],
    profile: {
        level: 'Senior',
        experience: '5年'
    },
    birthday: new Date('1990-01-01'),
    greet: function() {
        return `Hello, I'm ${this.name}`;
    }
};

// 使用 lodash 进行深拷贝
const copied = _.cloneDeep(original);

console.log(copied); // 完整的对象副本，包括函数和日期

// 测试深拷贝特性
original.skills.push('Vue');
original.profile.level = 'Expert';

console.log(copied.skills); // ['JavaScript', 'React'] - 不受影响
console.log(copied.profile.level); // 'Senior' - 不受影响
```

### 数组深拷贝

#### 1. JSON.parse(JSON.stringify())
对于数组也可以使用 JSON 方法进行深拷贝。

```javascript
const originalArray = [1, 2, 3, { name: '测试' }, [4, 5]];

// 使用 JSON 方法进行深拷贝
const copiedArray = JSON.parse(JSON.stringify(originalArray));

console.log(copiedArray); // [1, 2, 3, { name: '测试' }, [4, 5]]

// 修改原始数组中的引用类型
originalArray[3].name = '修改后的测试';
originalArray[4].push(6);

console.log(copiedArray[3].name); // '测试' - 不受影响
console.log(copiedArray[4]); // [4, 5] - 不受影响
```

#### 2. 使用 Lodash
```javascript
const originalArray = [1, 2, { name: '对象' }, [3, 4]];

// 使用 lodash 进行深拷贝
const copiedArray = _.cloneDeep(originalArray);

console.log(copiedArray); // [1, 2, { name: '对象' }, [3, 4]]

// 测试深拷贝特性
originalArray[2].name = '修改的对象';
originalArray[3].push(5);

console.log(copiedArray[2].name); // '对象' - 不受影响
console.log(copiedArray[3]); // [3, 4] - 不受影响
```

### 手动实现-深拷贝

```javascript

// 手动实现深拷贝步骤：
// 1. 判断参数是否为基本类型（如：string、number、boolean、null、undefined），如果是则直接返回该值
// 2. 如果是引用类型（对象或数组），则创建一个新的对象或数组
// 3. 遍历原对象的自身属性（不包括原型链上的属性）
// 4. 对每个属性值进行递归深拷贝（如果属性值是引用类型，继续递归拷贝）
// 5. 返回新对象

function deepCopy(obj) {

    // 1、用typeof判断参数类型
    // 这里把null单独拿出来是因为 typeof null 会返回 object，所以要先判断null。
    if (obj === null || typeof obj !== "object") {
        return obj;
    }

    // 2、用isArray判断是数组还是对象
    const newObj = Array.isArray(obj) ? [] : {};

    // 3、for in 开始遍历对象的所有属性
    for (let key in obj) {

        // 只复制对象自身属性。
        // 为啥只复制自身属性？因为如果不这样做，原型链上的属性也会被拷贝到新对象里，这样新对象就不再是原对象"自身"属性的简单副本了，可能会引入意料之外的属性，导致数据混乱。
        if (obj.hasOwnProperty(key)) {
            // 递归调用深拷贝函数，确保引用类型也被完全拷贝
            newObj[key] = deepCopy(obj[key]);
        }
    }

    // 4、返回新对象
    return newObj;

}


// 测试用例
const obj = {
    name: '张三',
    age: '28',
    hobby: ['篮球','足球','羽毛球'],
    info: {
        city:'济南'
    },
}

// 调用函数
const newObj = deepCopy(obj);

// 补充测试1：修改新对象的基本类型属性，不影响原对象
newObj.name = '李四';
console.log('修改newObj.name后：');
console.log('原对象name:', obj.name); // 期望输出：张三
console.log('新对象name:', newObj.name); // 期望输出：李四

// 补充测试2：修改新对象的引用类型属性，不会影响原对象
newObj.hobby.push('乒乓球');
console.log('修改newObj.hobby后：');
console.log('原对象hobby:', obj.hobby); // 期望输出：['篮球','足球','羽毛球']
console.log('新对象hobby:', newObj.hobby); // 期望输出：['篮球','足球','羽毛球','乒乓球']

// 补充测试3：修改嵌套对象的属性，不会影响原对象
newObj.info.city = '青岛';
console.log('修改newObj.info.city后：');
console.log('原对象info.city:', obj.info.city); // 期望输出：济南
console.log('新对象info.city:', newObj.info.city); // 期望输出：青岛

```

## 深拷贝的特点总结

1. **基本类型**：完全独立，修改不会相互影响
2. **引用类型**：完全独立，修改不会相互影响
3. **性能**：比浅拷贝性能较差，内存占用更多
4. **适用场景**：当需要完全独立的对象副本时使用
5. **递归特性**：会递归拷贝所有层级的引用类型

## 使用建议

- **简单对象**：推荐使用 `JSON.parse(JSON.stringify())` 方法
- **复杂对象**：推荐使用 Lodash 的 `cloneDeep` 方法
- **自定义需求**：可以手动实现递归深拷贝函数
- **性能考虑**：深拷贝比浅拷贝消耗更多资源，只在必要时使用
