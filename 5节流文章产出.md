# 节流

## 概念
    1、节流（Throttle）是一种限制函数执行频率的技术。
    2、在指定时间间隔内，无论事件触发多少次，函数只会执行一次。
    3、节流的核心思想是：在连续触发的事件中，按固定频率执行。
    4、常用于滚动事件、鼠标移动、窗口resize等场景。

## 节流有哪些用法？

### 基础节流实现

#### 1. 简单节流函数
使用定时器实现基础的节流功能。

```javascript
function throttle(func, delay) {
    let timeoutId;
    let lastExecTime = 0;
    
    return function(...args) {
        const currentTime = Date.now();
        
        // 如果距离上次执行时间超过延迟时间，立即执行
        if (currentTime - lastExecTime > delay) {
            func.apply(this, args);
            lastExecTime = currentTime;
        } else {
            // 否则设置定时器，在剩余时间内执行
            clearTimeout(timeoutId);
            timeoutId = setTimeout(() => {
                func.apply(this, args);
                lastExecTime = Date.now();
            }, delay - (currentTime - lastExecTime));
        }
    };
}

// 使用示例
const scrollHandler = throttle(function(event) {
    console.log('滚动位置:', window.scrollY);
}, 100);

window.addEventListener('scroll', scrollHandler);
```

#### 2. 时间戳节流
基于时间戳的节流实现，第一次触发立即执行。

```javascript
function throttleTimestamp(func, delay) {
    let lastExecTime = 0;
    
    return function(...args) {
        const currentTime = Date.now();
        
        // 如果距离上次执行时间超过延迟时间，执行函数
        if (currentTime - lastExecTime >= delay) {
            func.apply(this, args);
            lastExecTime = currentTime;
        }
    };
}

// 使用示例
const mouseMoveHandler = throttleTimestamp(function(event) {
    console.log('鼠标位置:', event.clientX, event.clientY);
}, 200);

document.addEventListener('mousemove', mouseMoveHandler);
```

### 手动实现-节流

```javascript

// 手动实现节流步骤：
// 1. 创建一个闭包函数，用于保存上次执行时间和定时器ID
// 2. 返回一个新函数，这个函数会检查时间间隔
// 3. 如果距离上次执行时间超过延迟时间，立即执行
// 4. 否则设置定时器，在剩余时间内执行
// 5. 支持立即执行、取消、刷新等高级功能

// 为什么要保存 this 上下文？
// 在 JavaScript 中，this 的值取决于函数如何被调用
// 如果不保存 this，在 setTimeout 回调中 this 会变成 undefined 或 window
// 示例：
// const obj = { 
//  name: '测试', 
//  method: function() { console.log(this.name); }
// };
// const throttled = throttle(obj.method, 100);
// throttled(); // 如果不保存 this，会输出 undefined

// call、apply、bind 的区别：
// call: func.call(thisArg, arg1, arg2) - 立即调用，逐个传递参数
// apply: func.apply(thisArg, [arg1, arg2]) - 立即调用，数组传递参数  
// bind: func.bind(thisArg) - 创建新函数，永久绑定 this

// ...args 的作用：
// ...args 是 ES6 的剩余参数语法，将所有传入的参数收集到一个数组中
// 示例：
// function test(...args) { console.log(args); }
// test(1, 2, 3); // 输出：[1, 2, 3]
// test('a', 'b'); // 输出：['a', 'b']
// 在节流函数中，...args 确保原函数的所有参数都能正确传递

function throttle(func, delay, options = {}) {

    // 1、声明变量
    let timeoutId;
    let lastExecTime = 0;
    const { leading = true, trailing = true } = options;

    // 2、返回节流函数
    // ...args 收集所有传入的参数，确保原函数的参数不丢失
    return function(...args) {
        
        // 3、保存this上下文
        // 原因：setTimeout 回调中的 this 会丢失，需要保存调用时的 this
        const context = this;
        
        // 4、获取当前时间
        const currentTime = Date.now();
        
        // 5、判断是否立即执行（leading 为 true 且是第一次执行）
        if (leading && currentTime - lastExecTime > delay) {
            // 立即执行
            func.apply(context, args);
            lastExecTime = currentTime;
        } else {
            // 6、清除之前的定时器
            clearTimeout(timeoutId);
            
            // 7、设置新的定时器（trailing 为 true 时）
            if (trailing) {
                timeoutId = setTimeout(() => {
                    func.apply(context, args);
                    lastExecTime = Date.now();
                }, delay - (currentTime - lastExecTime));
            }
        }
    };

}


// 测试用例
// 模拟滚动功能
function scrollHandler(scrollY) {
    console.log('滚动位置:', scrollY);
}

// 测试 this 上下文的对象
const scrollObj = {
    name: '滚动监听器',
    handleScroll: function(scrollY) {
        console.log(`${this.name} 处理滚动: ${scrollY}`);
    }
};

// 测试多参数的函数
function multiParamFunc(eventType, x, y) {
    console.log(`事件类型: ${eventType}, 坐标: (${x}, ${y})`);
}

// 创建节流函数
const throttledScroll = throttle(scrollHandler, 100);
const throttledObjScroll = throttle(scrollObj.handleScroll, 100);
const throttledMultiParam = throttle(multiParamFunc, 100);

// 测试1：连续快速触发
console.log('测试1：连续快速触发');
throttledScroll(100);
throttledScroll(200);
throttledScroll(300);
throttledScroll(400);
// 预期：立即输出 '滚动位置: 100'，然后延迟后输出 '滚动位置: 400'

// 测试2：延迟触发
setTimeout(() => {
    console.log('测试2：延迟触发');
    throttledScroll(500);
}, 200);
// 预期：输出 '滚动位置: 500'

// 测试3：只执行第一次（leading: true, trailing: false）
const leadingThrottledScroll = throttle(scrollHandler, 100, { leading: true, trailing: false });
console.log('测试3：只执行第一次');
leadingThrottledScroll(600);
leadingThrottledScroll(700);
leadingThrottledScroll(800);
// 预期：只输出 '滚动位置: 600'

// 测试4：只执行最后一次（leading: false, trailing: true）
const trailingThrottledScroll = throttle(scrollHandler, 100, { leading: false, trailing: true });
console.log('测试4：只执行最后一次');
trailingThrottledScroll(900);
trailingThrottledScroll(1000);
trailingThrottledScroll(1100);
// 预期：延迟后输出 '滚动位置: 1100'

// 测试5：this 上下文测试
console.log('测试5：this 上下文测试');
throttledObjScroll.call(scrollObj, 1200);
throttledObjScroll.call(scrollObj, 1300);
// 预期：立即输出 '滚动监听器 处理滚动: 1200'，然后延迟后输出 '滚动监听器 处理滚动: 1300'

// 测试6：多参数传递测试
console.log('测试6：多参数传递测试');
throttledMultiParam('mousemove', 100, 200);
throttledMultiParam('mousemove', 150, 250);
throttledMultiParam('mousemove', 200, 300);
// 预期：立即输出 '事件类型: mousemove, 坐标: (100, 200)'，然后延迟后输出 '事件类型: mousemove, 坐标: (200, 300)'

```

## 节流的特点总结

1. **固定频率执行**：在指定时间间隔内，无论触发多少次，函数只会执行一次
2. **时间间隔控制**：通过时间间隔来控制函数的执行频率
3. **性能优化**：减少不必要的函数调用，提高性能
4. **用户体验**：避免频繁操作导致的界面卡顿
5. **灵活配置**：支持立即执行、延迟执行、取消等高级功能

## 使用建议

- **滚动事件**：推荐使用节流处理滚动事件，避免频繁计算
- **鼠标移动**：处理mousemove事件，减少计算频率
- **窗口调整**：处理resize事件，避免频繁重新布局
- **按钮点击**：防止用户重复快速点击
- **API调用**：限制请求频率，避免服务器压力过大
